# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Test artifacts
sources/*/schema_validation_results.json
*.pyc

# OS
.DS_Store
Thumbs.db

# Claude
.claude/agent-memory-local/

# Git worktrees
.worktrees/

# Dev config
tests/unit/sources/*/configs/dev_config.json

# companies_house auth_test.py defaults to a repo-root credentials.json when
# CONNECTOR_TEST_CONFIG_JSON/_PATH aren't set — never let a live api_key land here.
/credentials.json

# Replay config — fake credentials for simulate/replay mode. The canonical
# source is the ``replay_config`` class attribute on each test class
# (``LakeflowConnectTests`` writes them there); this glob lets contributors
# drop a local override JSON next to a connector to override per run.
tests/unit/sources/*/configs/replay_config.json

# Source-simulator artifacts written by live runs (cassette, coverage report,
# spec-validation report). Raw cassettes are regenerated each time and not
# part of the source of truth — the corpus under source_simulator/specs/<source>/ is.
# However, validation and coverage reports ARE committed as record-mode evidence
# for self-review checks B11/B12.
tests/unit/sources/*/cassettes/*.json
!tests/unit/sources/*/cassettes/*.json.validation.json
!tests/unit/sources/*/cassettes/*.json.coverage.json

# Self-review reports (output of the self-review-connector skill). Local
# checkpoint for a developer; not committed by default.
tests/unit/sources/*/SELF_REVIEW.md
